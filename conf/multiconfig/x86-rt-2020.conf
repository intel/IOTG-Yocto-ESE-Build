# this is the standard full sized image with LTS RT kernel as the default boot option
SELECTABLE_KERNEL_DEFAULT = "x86-lts-rt-2020-kernel-default.inc"

# common contents and settings
include x86-2020-image.inc
